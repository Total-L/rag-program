apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-program.fullname" . }}
  labels:
    {{- include "rag-program.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-program.selectorLabels" . | nindent 4 }}